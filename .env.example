# Admin Panel Environment Configuration
# Copy this file to .env and update values accordingly

# Backend API URL (include /api/v1 prefix)
VITE_API_URL=http://localhost:8001/api/v1

# Frontend port (default: 4001)
# This is configured in vite.config.ts, change there if needed
